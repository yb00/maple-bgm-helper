import React from 'react';

import './Navbar.scss';

const Navbar = () => {
    return (
        <nav>
            <h1 className="navbar-brand">Maple Helper Tools</h1>
        </nav>
    );
};

export default Navbar;
